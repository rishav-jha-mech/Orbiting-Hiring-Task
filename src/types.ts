export type MeetupEventType = {
    time: string,
    title: string,
    location: string,
    description: string,
    images: string[],
    image: string,
    link: string,
    attendees: string,
    isOnlineEvent: boolean,
}